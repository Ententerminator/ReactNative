import React from 'react';
import {
  StyleSheet,
  Button,
  View,
  SafeAreaView,
  Text,
  Alert,
} from 'react-native';

const Camera = ({navigation}) => {
    return (
    <View>
      <Text > Camera </Text>
    </View>
    );
  }

  export default Camera;